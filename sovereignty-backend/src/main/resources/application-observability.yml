spring:
  kafka:
    listener:
      observation-enabled: true
    template:
      observation-enabled: true

management:
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
  tracing:
    sampling:
      probability: 1.0

logging:
  file:
    name: logs/sovereignty-backend.log
